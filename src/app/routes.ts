enum Route {
  Main = "/",
  Login = "/login",
  Registration = "/register",
  GraphQL = "/GRAPHQL",
  Get = "/GET",
  Post = "/POST",
  RESTfull = "/restfull",
  History = "/history",
}

export { Route };
